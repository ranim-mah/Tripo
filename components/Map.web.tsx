import React from "react";
import { Text, View } from "react-native";

const Map = () => {
  return (
    <View className="w-full h-full rounded-2xl items-center justify-center bg-general-500">
      <Text className="text-sm text-general-200">
        Map is available on mobile only.
      </Text>
    </View>
  );
};

export default Map;
